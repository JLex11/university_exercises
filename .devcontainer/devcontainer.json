{
  "name": "TypeScript with Bun Development",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:latest",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "WallabyJs.console-ninja"
      ]
    }
  },
  "postCreateCommand": "curl -fsSL https://bun.sh/install | bash && echo 'export PATH=$HOME/.bun/bin:$PATH' >> ~/.bashrc && echo 'export BUN_INSTALL=$HOME/.bun' >> ~/.bashrc",
  "remoteUser": "node",
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:${containerEnv:HOME}/.bun/bin"
  }
}
